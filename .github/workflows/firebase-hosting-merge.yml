# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting and Functions on merge
on:
  push:
    branches:
      - master # ou 'main' si c'est votre branche principale
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm ci && npm run build # Assurez-vous que 'npm run build' construit aussi vos fonctions (Genkit le fait souvent)
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: ${{ secrets.GITHUB_TOKEN }}
          firebaseServiceAccount: ${{ secrets.FIREBASE_SERVICE_ACCOUNT_VOICE_WEAVER_81QNS }}
          channelId: live
          projectId: voice-weaver-81qns
          args: deploy --only hosting,functions # Déploie explicitement hosting ET functions
